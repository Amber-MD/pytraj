sudo: required
services:
  - docker

matrix:
  include:
   - { os: osx }

before_install:
    - docker pull hainm/pytraj-build-box

install:
    - source devtools/build_all_wheels.sh

script:
    - echo "hello"

after_success:
    - echo "after_success"

notifications:
  email: false
