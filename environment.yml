channels:
    - conda-forge
name: pytraj-gha
dependencies:
    - pyflakes
    - numpy=2.0.0
    - cython=3.0.11
    - parmed
    - pytest
    - mock
    - pytest

    - gcc_linux-64
    - gxx_linux-64
    - gfortran
    - libnetcdf
    - lapack
    - fftw
    - make
    - binutils

    # libcpptraj: bash configure --requires-flink -shared -noarpack --with-bzlib=/opt/conda \
    # --with-zlib=/opt/conda -nosanderlib -nohdf5 --with-netcdf=/opt/conda --with-lapack=/opt/conda \
    # --with-fftw3=/opt/conda
