channels:
    - conda-forge
dependencies:
    - pyflakes
    - numpy=2.0.0 # > 2.0 will cause Frame.__array__() takes no keyword arguments (so on)
    - cython=0.29
    - parmed
    - pytest
    - mock
